# Server Configuration
PORT=5001
NODE_ENV=development
APP_URL=http://localhost:3000
BACKEND_URL=http://localhost:5001
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=intern_registration
DB_PORT=3306
DB_CONNECTION_LIMIT=10
DB_TIMEZONE=+07:00
DB_SSL_ENABLED=false
DB_SSL_REJECT_UNAUTHORIZED=true

# JWT Configuration
JWT_SECRET=replace_with_secure_random_value
JWT_EXPIRES_IN=24h
VERIFICATION_TOKEN_EXPIRY_HOURS=24
PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=30

# Rate Limiter
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# Email Configuration
# SMTP_HOST=mail.fitalenta.co.id
# SMTP_PORT=465
# SMTP_SECURE=true          # port 465 biasanya memakai TLS implicit; gunakan "false" jika Anda pindah ke port STARTTLS (587)
# SMTP_USER=registration@fitalenta.co.id
# SMTP_PASSWORD=infowebsite
# SMTP_FROM="Fitalenta <no-reply@fitalenta.co.id>"
# SMTP_DOMAIN=fitalenta.co.id               # hanya jika kode Anda memang membutuhkannya
# SMTP_TLS_REJECT_UNAUTHORIZED=true

SMTP_MAILER=smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465 
SMTP_USER=inventramasjid@gmail.com
# SMTP_USERNAME=bidangdakwah.salmanitb@gmail.com
# SMTP_PASSWORD=Bd1salmanitb
SMTP_PASS=inventra123
# SMTP_PASSWORD=dpeptlzqmlckyjld
# SMTP_ENCRYPTION=ssl
SMTP_ENCRYPTION=tls
SMTP_FROM_ADDRESS=bidangdakwah.salmanitb@gmail.com
SMTP_FROM_NAME="${APP_NAME}"

# File Upload Configuration
FILE_BASE_URL=http://localhost:5001
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# Frontend Integration
VITE_API_BASE_URL=http://localhost:5001
VITE_FILE_BASE_URL=http://localhost:5001